# Variables de entorno para la aplicación Anima Counter
# Copia este archivo a .env y modifica los valores según tu configuración

# PostgreSQL Database (usado directamente por el contenedor de PostgreSQL)
POSTGRES_DB=anima_counter
POSTGRES_USER=anima_user
POSTGRES_PASSWORD=anima_password_2024
POSTGRES_HOST_AUTH_METHOD=trust

# Base de datos para conexión desde aplicaciones (backend/frontend)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=anima_counter
DB_USER=anima_user
DB_PASSWORD=anima_password_2024

# Backend API
NODE_ENV=production
BACKEND_PORT=3000
FRONTEND_URL=http://frontend

# Frontend
APP_PORT=80

# Adminer (herramienta de administración de BD)
ADMINER_DEFAULT_SERVER=postgres

# URLs de conexión
DATABASE_URL=postgresql://anima_user:anima_password_2024@postgres:5432/anima_counter

# NOTAS:
# - Para desarrollo local, cambia DB_HOST a 'localhost'
# - Para producción, usa contraseñas seguras
# - Puedes cambiar los puertos si hay conflictos