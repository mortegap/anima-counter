# ==========================================
# ANIMA COUNTER - Environment Variables
# ==========================================
# Copia este archivo a .env y modifica los valores según tu configuración

# PostgreSQL Database
POSTGRES_DB=anima_counter
POSTGRES_USER=anima_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_HOST_AUTH_METHOD=md5

# Database Connection (para backend)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=anima_counter
DB_USER=anima_user
DB_PASSWORD=your_secure_password_here

# Backend
NODE_ENV=production
BACKEND_PORT=3000
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_here_min_32_chars
FRONTEND_URL=http://localhost

# Frontend
APP_PORT=80

# Adminer
ADMINER_DEFAULT_SERVER=postgres

# ==========================================
# NOTAS DE SEGURIDAD
# ==========================================
# 1. NUNCA uses estas credenciales por defecto en producción
# 2. El JWT_SECRET debe tener al menos 32 caracteres
# 3. Usa contraseñas fuertes y únicas
# 4. Para desarrollo, copia .env.dev.example a .env
# 5. NO versiones el archivo .env en git